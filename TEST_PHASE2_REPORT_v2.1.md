# MiniRock Phase 2 测试报告（修复后重跑）

**测试时间**: 2026-03-15  
**测试工程师**: 小七 (灵犀)  
**测试范围**: TC-AI-001 ~ TC-AI-005  
**测试环境**: http://43.160.193.165/frontend/

---

## 修复验证结果

### ✅ 已修复问题

| Bug | 描述 | 验证结果 |
|-----|------|----------|
| AI-BUG-005 | `stock_detail` 路由未注册 | ✅ `/api/stock/*` 路由已可用 |
| AI-BUG-006 | API 路径前缀不匹配 | ✅ 统一为 `/api/*` |
| AI-BUG-007 | 前端 `/api/api/` 重复路径 | ✅ 已修复 |

---

## API 测试结果（TC-AI-005）

### ✅ 正常 API（5/7）

| API 端点 | 方法 | 状态 | 响应时间 | 说明 |
|----------|------|------|----------|------|
| `/api/portfolio/holdings` | GET | ✅ 正常 | <100ms | 返回 demo_user 数据 |
| `/api/ai/analyze-stock` | POST | ✅ 正常 | ~15s | Kimi分析成功，448字符 |
| `/api/ai/scenario/{code}` | GET | ✅ 正常 | <1s | 返回3个情景 |
| `/tushare/quote/{code}` | GET | ✅ 正常 | <500ms | 行情数据完整 |
| `/auth/login` | POST | ✅ 正常 | <100ms | 认证接口正常 |

### ⚠️ 部分正常 API（2/7）

| API 端点 | 方法 | 状态 | 问题 |
|----------|------|------|------|
| `/api/stock/basic/{code}` | GET | ⚠️ 数据缺失 | 路由正常，数据库缺少股票基础信息 |
| `/api/stock/kline/{code}` | GET | ⚠️ 表缺失 | 路由正常，`stock_daily` 表不存在 |
| `/api/stock/capital/{code}` | GET | ⚠️ 表缺失 | 路由正常，`stock_hsgt_hold` 表不存在 |
| `/api/ai/news-related/{code}` | GET | ⚠️ 编码问题 | URL编码错误 |
| `/tushare/search` | GET | ⚠️ 编码问题 | URL编码错误 |

### 认证状态

```
账号: 13800138000
状态: 已注册
密码: 不正确（需要正确密码）

Demo账号: 无法使用（格式校验失败）
```

---

## 前端页面测试

### TC-AI-001: 个股诊断页面
**状态**: ⏸️ **阻塞**

**阻塞原因**: 页面需要登录认证
- 访问 `stock-detail.html?code=000001.SZ` 自动重定向到登录页
- 测试账号 `13800138000` 密码不正确
- 无法验证页面完整功能

### TC-AI-002: 组合分析页面
**状态**: ⚠️ **部分通过**

**验证结果**:
- API 响应正常 ✅
- 但 `demo_user` 持仓数据为空
- 需要初始化测试数据

### TC-AI-003: 情景推演
**状态**: ✅ **通过**

**API 验证**:
```json
{
  "symbol": "000001.SZ",
  "scenarios": {
    "optimistic": {"change_percent": 15, ...},
    "neutral": {"change_percent": 5, ...},
    "pessimistic": {"change_percent": -15, ...}
  }
}
```

**注意**: `current_price: 0.0` 仍需修复

### TC-AI-004: 新闻关联
**状态**: ⚠️ **部分通过**

- AI 分析 API 返回相关新闻 ✅
- 独立新闻关联 API 有编码问题

---

## Bug 状态更新

| 编号 | 优先级 | 原问题 | 当前状态 |
|------|--------|--------|----------|
| AI-BUG-005 | P0 | stock_detail 路由未注册 | ✅ **已修复** |
| AI-BUG-006 | P1 | API 路径前缀不匹配 | ✅ **已修复** |
| AI-BUG-007 | P2 | 前端重复 `/api` 路径 | ✅ **已修复** |
| AI-BUG-002 | P1 | demo_user 持仓数据为空 | ⏸️ **待补充数据** |
| AI-BUG-003 | P2 | 情景推演 current_price=0 | ⏸️ **待修复** |
| AI-BUG-008 | P2 | 新闻/搜索 API URL编码问题 | 🆕 **待修复** |
| AI-BUG-009 | P1 | 数据库表缺失 | 🆕 **待创建** |

---

## 待开发团队完成

### 高优先级

1. **补充测试账号密码** (灵犀)
   - 提供 `13800138000` 的正确密码，或创建新的测试账号

2. **补充数据库表** (灵犀)
   ```sql
   -- 需要创建的表
   - stock_basic (股票基础信息)
   - stock_daily (K线数据)
   - stock_hsgt_hold (北向资金)
   ```

3. **初始化测试数据** (灵犀)
   - 为 `demo_user` 添加持仓数据
   - 补充股票基础数据到 `stock_basic`

### 中优先级

4. **修复情景推演 current_price** (灵犀)
   - 从 tushare 或本地数据库获取实时价格

5. **修复 URL 编码问题** (灵犀)
   - 中文参数编码处理

---

## 结论

**路由修复成功** ✅
- 所有 `/api/*` 路由已正确注册
- 前端 API 路径已修复

**数据层阻塞** ⏸️
- 缺少测试账号密码
- 缺少数据库表和数据
- 无法进行完整功能测试

**建议**: 开发团队补充测试数据后，重新进行完整功能测试。

---

*报告生成时间: 2026-03-15 20:50*
