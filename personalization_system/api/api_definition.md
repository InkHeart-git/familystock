# 个性化话术系统API接口定义

## 1. 用户行为上报接口
**接口地址**：POST /api/personalization/behavior/report  
**请求参数**：

**响应**：


## 2. 获取用户风格偏好接口
**接口地址**：GET /api/personalization/style/:user_id  
**响应**：


## 3. 生成个性化分析接口
**接口地址**：POST /api/personalization/analysis/generate  
**请求参数**：

**响应**：

